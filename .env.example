# ===== 填这里。这个文件已在 .gitignore 里，不会被提交 =====

# --- OpenAI 兼容接口（大多数中转站走这个）---
# 中转站地址，一般以 /v1 结尾，例如 https://api.xxx.com/v1
OPENAI_BASE_URL=
OPENAI_API_KEY=

# --- Anthropic 原生接口（如果中转站支持 /v1/messages）---
# 例如 https://api.xxx.com  （不要加 /v1）
ANTHROPIC_BASE_URL=
ANTHROPIC_API_KEY=
