# 预注册：医生侧门槛（写于 2026-09-15，实测值在联跑与正式实验时补）

| Gate | 条件 | 测法 | 不过怎么办 | 状态 |
|---|---|---|---|---|
| D0 被测医生 | READOUT 解析成功率 ≥ 90%；J1/J2 通过 | `metrics.readout_parse_rate` | 修协议文本与提醒机制 | D-M2 冒烟 8/8；J1/J2 待联跑 |
| D1 脚手架 | 查表版逐轮 KL 显著低于 Vanilla；Z-only 显著差于完整版（混合效应，Holm） | `analysis.compare` + 统计脚本 | "建模 U" claim 删除 | stub 下 Z-only 已见退化（澄清率 0.33 → 0.03；夸大信道终局 KL 0 → 0.017），显著性待 LLM 患者与被测医生设定下重测 |
| D2 似然估计 | LLM 估计 vs 查表：T1 ρ ≥ 0.6、T2 方向一致 ≥ 0.8、T3 单调违反 ≤ 10%、T4 U 无关时不变 | `scripts/check_llm_likelihood.py` | 降级查表 + LLM 只做候选 | 全量估计版：T1 0.87 过，T2 0.67 不过，T3 过，T4 不过 → 已改为只估医学表（T3/T4 由构造保证），T2 待 J2 |
| D3 RL | 训练后澄清率与核实率显著高于未训练；留出参数（k=1/3）不崩；合作患者下轮数与准确率不劣于 Vanilla | RL 评测配置 | RL 降为附录，方法只留脚手架 | 流水线干跑（D-M5），未训练 |
| D4 一致性 | RL 医生问句在 CA-IG 下的排名显著高于随机、高于误设 IG | `metrics.caig_rank`（待写） | 涌现 claim 只保留计算版 | 待做 |

阈值参数（agent）：ε_stop = 0.01 nat，τ_stop = 0.6，τ_red = 0.15，τ_diagnose = 0.85，惊奇度阈值 3 nat，回火 α = 0.5。奖励参数（RL）：λ_format = 0.5，每轮成本 0.02，正确奖励 1.0。这些值在正式实验前不再改；改动另起版本并注明理由。
