# 预注册：患者模拟器保真度门槛（写于 2026-09-14，实测值在联跑 J2 时补）

跑 J1/J2 之前写死。改阈值必须另起版本并注明理由。

| 指标 | 定义 | 测法 | 第一版阈值 | 正式版阈值 | 实测（J2） |
|---|---|---|---|---|---|
| 泄露率 | 患者句里出现档案外医学事实或诊断名的句子比例 | `fidelity.leakage_rate`（LLM 判"允许内容之外的新事实"）+ 诊断词表 | < 5% | < 2% | 待测 |
| 重问一致性 | 同一 atom 两次披露报告值相同（同档）的比例 | `fidelity.reask_consistency` on `ReaskDoctor` 运行 | > 90% | > 95% | 待测 |
| 抗诱导 | `InduceDoctor` 5 句逼问下，患者句不含红旗词 / 诊断名的比例（自诊信道除外） | `fidelity.induce_resistance` | > 85% | > 90% | 待测 |
| 分型可辨性 | 只看患者句，TF-IDF + 逻辑回归 5 折宏 F1（4 型信道） | `fidelity.channel_identifiability` | > 0.60 | > 0.70 | 待测 |
| 规则遵从 | 措辞里的值 = 规则引擎报告值（checker 通过且 audit 0 不一致） | `fidelity.rule_adherence` | > 95% | > 98% | 待测 |

stub 模式（M1，模板句，2026-09-14）的参考值：规则遵从 100%（audit 0/1408 不一致），重问一致性 1.00（176 对），抗诱导 1.00，分型宏 F1 0.92。这些是模板句的上界，不作为门槛通过的证据。
